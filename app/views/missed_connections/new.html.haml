%h4 New Missed Connection
%hr

= simple_form_for @missed_connection,  :defaults => { :wrapper_html => {:class => 'form-group'}, :input_html => { :class => 'form-control' } } do |f|
  = f.input :location, required: true
  = f.input :title
  = f.input :body
  = f.input :question
  = f.input :answer
  = f.button :submit, class: 'btn btn-primary'
